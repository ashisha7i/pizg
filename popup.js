document.addEventListener('DOMContentLoaded',function(event){document.getElementById("btnXml").addEventListener('click', function (event) {let textareaElement = document.getElementById("results");let valueXml = textareaElement.value; textareaElement.value = vkbeautify.xml(valueXml);});getResults();}); function getResults(){chrome.tabs.query({ active: true, currentWindow: true }, function (tabs) {chrome.tabs.sendMessage(tabs[0].id, { action: 'clicked_browser_action' }, function (response) { showResults(response.results); });});} function showResults(results) {var resultsElement = document.getElementById('results'); resultsElement.innerText = results;} function prettyPrint(dataType) {chrome.tabs.query({ active: true, currentWindow: true }, function (tabs) {chrome.tabs.sendMessage(tabs[0].id, { action: 'prettify_action', type: dataType }, function (response) {showResults(response.results);});});}
